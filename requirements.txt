psutil>=5.9.4          # system has 7.0.0, that's fine
picamera2>=0.3.21; sys_platform == "linux"
Twisted>=24.7.0
cryptography>=43,<45
opencv-python          # if wheel fails, use: sudo apt-get install -y python3-opencv
pyserial==3.5
pyzmq>=26,<27
msgpack>=1.0,<2
# numpy from PyPI uses Apple Accelerate on macOS ARM64 (no OpenBLAS, no Homebrew
# libomp). Without this, --system-site-packages inherits Homebrew numpy which
# links OpenBLAS → Homebrew libomp, causing two libomp instances to load
# simultaneously with torch's bundled libomp → SIGSEGV in __kmp_fork_call.
numpy>=1.26
scipy>=1.15,<2
ultralytics>=8.3,<9

# Desktop GUI (src/gui/) — single-window app that replaces the
# Angular frontend. PyQt5 is the toolkit (QGraphicsView for the map, QLabel
# for camera frames, QWizard for the calibration flow). pyqtgraph is the
# rolling chart used in the speed/steer panel and the calibration polynomial
# visualizer. The GUI talks directly to the bus over tcp:// (ZMQ telemetry +
# commands) and over UDP for video — no SocketIO bridge any more.
PyQt5>=5.15,<6
pyqtgraph>=0.13,<1

# Localization (EKF7) — Phase 2 of the Autoware-inspired refactor.
# filterpy provides ExtendedKalmanFilter with battle-tested numerics
# (Joseph form covariance update, no need to roll our own).
# pyproj converts WGS84 GPS lat/lon to a local ENU tangent plane so the
# filter operates in meters instead of degrees.
filterpy>=1.4.5,<2
pyproj>=3.6,<4

# ONNX Runtime — required by ultralytics when loading .onnx models (e.g. lane segmentation).
onnxruntime>=1.16.0
# For Raspberry Pi / generic ARM: pip install onnxruntime
# For Jetson (JetPack 6 / L4T R36): pip install --extra-index-url https://pypi.jetson-ai-lab.dev/jp6/cu126 onnxruntime-gpu

# Optional: Traffic sign detection (MobilenetV2 SSD)
# ai-edge-litert         # Recommended (Google LiteRT, supports Python 3.13+)
# tflite-runtime          # Alternative (legacy, may not have Python 3.13 wheels)
# For Raspberry Pi: pip install ai-edge-litert
