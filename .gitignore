docs/build/

# Runtime logs and recordings (large and machine-local)
*.log
*.jsonl
*.stdout
*.stderr
logs/
**/logs/
temp/logs/
output_video*.avi
temp/**/*.avi

# Campaign / line-following dumps (runtime; can exceed GitHub's 100MB limit)
temp/line_following_*_last_run.txt
temp/**/*_last_run.txt

# Legacy Angular build cache (do not commit)
legacy/dashboard-frontend/.angular/

.DS_Store
.vscode/
__pycache__
**/*.pyc
/src/dashboard/frontend/node_modules
src/dashboard/frontend/.angular/
src/dashboard/frontend/dist/
**/venv/
.venv/
**/.venv/
aiserver/models/supercombo.onnx
src/dashboard/frontend/.angular/cache/18.2.21/dashboard/.tsbuildinfo

# GUI layout (user-specific)
config/gui_layout.json

# Acados generated code (built locally)
src/control/c_generated_code/

# BFMC plans (operator drops planner outputs here; not source of truth)
plans/*.json

# Legacy lanelet2 OSM (kept as backup after switching to the unified planner OSM)
maps/*/lanelet2_map.legacy.osm
